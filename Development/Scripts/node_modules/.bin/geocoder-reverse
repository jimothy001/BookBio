#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-geocoder/bin/geocoder-reverse" "$@"
  ret=$?
else 
  node  "$basedir/../node-geocoder/bin/geocoder-reverse" "$@"
  ret=$?
fi
exit $ret
